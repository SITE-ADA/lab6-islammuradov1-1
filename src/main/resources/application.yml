spring:
  application:
    name: lab4
  datasource:
    url: jdbc:h2:mem:productDB
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
